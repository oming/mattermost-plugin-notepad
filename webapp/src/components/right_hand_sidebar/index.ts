// import {connect} from 'react-redux';

// import {Dispatch, bindActionCreators} from 'redux';

// import {getBookmarkSettings} from './selectors';
// import {fetchBookmarkSettings} from './actions';

// import RHSView from './components';
// import { GlobalState } from 'mattermost-redux/types/store';

// const mapStateToProps = (state: GlobalState) => {
//     console.log('hsan', state);

//     return {
//         contents: getBookmarkSettings(state) || {},
//     };
// };

// const mapDispatchToProps = (dispatch: Dispatch) => bindActionCreators({
//     fetchBookmarkSettings,
// }, dispatch);

// export default connect(mapStateToProps, mapDispatchToProps)(RHSView);

// eslint-disable-next-line @typescript-eslint/no-empty-function
export default function testAAA() {}
